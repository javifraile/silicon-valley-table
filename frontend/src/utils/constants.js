const BASE_URL = 'http://localhost:3001';

const PATH_EPISODES_URL = '/episodes';

const LOADING_MESSAGE = 'Loading data';

const ERROR_MESSAGE = 'Couldn&quot;t retrieve episodes';

const NO_MATCHES_MESSAGE = 'No matches';

export {
    BASE_URL,
    PATH_EPISODES_URL,
    LOADING_MESSAGE,
    ERROR_MESSAGE,
    NO_MATCHES_MESSAGE,
};
